[project]
name = "turboquant-autonomous-mesh"
version = "0.1.0"
description = "TurboQuant autonomous mesh"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.115,<1.0",
]

[project.scripts]
app = "app:app"
